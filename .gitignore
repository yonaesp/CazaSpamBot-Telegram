# Secretos y credenciales
.env
.env.*
!.env.example
*.session
*.session-journal
.cookie_key
cookies.enc

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
*.egg-info/
dist/
build/

# Welcomes personalizados por grupo (los genéricos versionados SÍ se suben:
# generic.txt y los de cada idioma, generic.en.txt, generic.fr.txt...)
config/welcomes/*.txt
!config/welcomes/generic.txt
!config/welcomes/generic.*.txt

# Terminos de spam que el admin anade desde Telegram: los gestiona el bot y son
# propios de cada despliegue. Las listas del repo (config/blacklist/*.txt) SI se
# versionan; estas no, para que un git pull nunca pise el trabajo del admin.
config/blacklist/custom/

# Notas y herramientas personales del despliegue original (no útiles a terceros)
SETUP-INICIAL.txt
scripts/rose_inspector.py

# Claude Code: config personal del workspace (opcional, no necesaria para el bot)
.claude/

# Data / logs
data/
logs/
*.db
*.db-journal
*.db-wal
*.db-shm
*.log

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code (local-only state — la carpeta .claude/ SI se commitea, las settings.local.json NO)
.claude/settings.local.json
.claude/cache/
