# Telegram Bot API (async)
python-telegram-bot[ext]==22.8

# Async HTTP (CAS lookup + notificaciones)
aiohttp==3.10.10

# Telethon (cliente MTProto). NO es solo para scripts: lo usan en runtime
# reporter.py, user_signals.py, telethon_bridge.py y story_reader.py, o sea
# reportes oficiales, bio/fotos de perfil, borrados y lectura de historias.
telethon==1.44.0

# Variables de entorno
python-dotenv==1.0.1

# Logging estructurado
structlog==24.4.0

# UTS#39 confusables (detección de nombres decorativos/homográficos anti-FP)
confusable-homoglyphs==3.3.1

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0

# Lint
ruff==0.7.4
